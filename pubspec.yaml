name: enhanced_pagination_view
description: "Powerful pagination package with O(1) item updates, dual mode support (infinite scroll + pagination buttons), and comprehensive state management."
version: 1.2.0
homepage: https://github.com/DevsLoom/enhanced_pagination_view
repository: https://github.com/DevsLoom/enhanced_pagination_view
issue_tracker: https://github.com/DevsLoom/enhanced_pagination_view/issues

environment:
  sdk: ">=3.10.1 <4.0.0"
  flutter: ">=3.0.0"

topics:
  - pagination
  - infinite-scroll
  - listview
  - ui
  - widget

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
